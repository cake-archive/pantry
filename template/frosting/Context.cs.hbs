using Cake.Core;
using Cake.Frosting;

public class {{{ cake.frosting.context }}} : FrostingContext
{
    public {{{cake.frosting.context}}}(ICakeContext context) 
        : base(context)
    {
    }
}