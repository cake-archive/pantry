using Cake.Common.Diagnostics;
using Cake.Frosting;

[TaskName("{{name}}")]
{{#each dependencies}}
[Dependency(typeof({{this}}))]
{{/each}}
public sealed class {{name}} : FrostingTask<Context>
{
    {{#unless skipRun }}
    public override void Run(Context context)
    {
        context.Information("Hello World");
    }
    {{/unless}}
}