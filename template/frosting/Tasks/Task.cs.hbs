using Cake.Common.Diagnostics;
using Cake.Frosting;

[TaskName("{{{name}}}")]
{{#each dependencies}}
[Dependency(typeof({{{this}}}))]
{{/each}}
public sealed class {{{name}}} : FrostingTask<{{{ cake.frosting.context }}}>
{
    {{#if runBody }}
    {{{runBody}}}
    {{/if}}
    {{#unless skipRun }}
    public override void Run({{{ cake.frosting.context }}} context)
    {
        context.Information("Hello World");
    }
    {{/unless}}
}